# sample config file

# Do not un-comment the next two lines, documentation only.
# idle_load_threshold - The device is considered busy if load is more than this value (%)
# idle_time_threshold - The device is considered idle if the load is less than idle_load_threshold for this period of time (minutes)

[setting]
# Will not suspend if any of these IPv4s are online, use an empty string "" to disable
ips_to_watch = "192.168.1.50" 
# Times when the machine should be awake in hh:mm, use an empty string "" to disable
wake_at = "1:00,16:00"
# Sleep modes are: 'standby' (ACPI S1), 'mem' (ACPI S3) or 'disk' (ACPI S4), default is 'mem'
sleep_mode = "mem"

[tuning]
# Check if the machine is idle every (x minute) (default is 1)
check_if_idle_every = 1
# If any machine from 'ips_to_watch' is online, stop monitoring for (x minute) (default is 5)
stop_monitoring_for = 5
# Reset the timers if the machine was busy for more than (x minute) (default is 3)
reset_monitoring_after = 3
# Suspend the machine if it has been idle for more than (x minute) (default is 15)
suspend_after = 15

[devices_to_monitor]
# Monitor all the attached disks and only suspend the machine if they are all idle (default is false)
all_disks = false

[devices_to_monitor.cpu]
# Machine will not suspend if the cpu is busy (default is true)
no_suspend_if_not_idle = true
# Default is 5%
idle_load_threshold = 5
# Default is 1 minute
idle_time_threshold = 1

# # Example disk entries (uncomment and add multiple [[...]] sections as needed)
# [[devices_to_monitor.disks]]
# # Device name
# name = "sda1"
# # Or use device uuid
# uuid = ""
# # The machine will not suspend if this device is busy (default is true)
# no_suspend_if_not_idle = true
# # Spin the drive down if it's idle (default is false)
# spind_down_if_idle = false
# # Default is 5% 
# idle_load_threshold = 5
# # Default is 1 minute
# idle_time_threshold = 1

